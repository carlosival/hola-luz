import { PresentationService } from './presentation.service.imp';

describe('PresentationService', () => {
  it('should be defined', () => {
    expect(new PresentationService()).toBeDefined();
  });
});
